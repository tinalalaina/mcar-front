name: Build and Push Docker Image

on:
  push:
    branches:
      - main
      - prod
  pull_request:
    branches:
      - main
      - prod
  workflow_dispatch: # Allow manual triggers

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      docker:
        image: docker:24.0.7-dind
        options: --privileged

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        run: docker login -u shop784 -p ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Extracts metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: shop784/mcar-front

      - name: Build and push Docker image
        run: |
          docker build -t shop784/mcar-front:latest .
          docker push shop784/mcar-front:latest


  # deploy:
  #     runs-on: ubuntu-latest
  #     needs: build
  #     steps:
  #     # Déployez sur le serveur distant
  #     - name: Deploy Docker image to remote server
  #       run: |
  #         sudo apt-get update && sudo apt-get install -y sshpass
  #         sshpass -p "${{ secrets.SERVER_PASSWORD }}" ssh -o StrictHostKeyChecking=no root@${{ secrets.SERVER_IP }} <<EOF
  #           docker pull ${{ secrets.DOCKER_USERNAME }}/mcar-front:latest
  #           docker stop andersonfrontend || true
  #           docker rm andersonfrontend || true
  #           docker system prune -f

  #           docker run -d \
  #             --name andersonfrontend \
  #             --network anderson_network \
  #             --ip=172.19.0.2 \
  #             --restart unless-stopped \
  #             -p 3000:3000 \
  #             shop784/mcar-front:latest 
  #         EOF